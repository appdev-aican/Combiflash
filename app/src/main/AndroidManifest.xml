<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.combiflash">

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".GraphActivity"
            android:exported="false" />
        <activity
            android:name=".LaunchCameraDualActivity"
            android:exported="false" />
        <activity android:name=".ScanQRCodeActivity" />
        <activity android:name=".LinkedDeviceActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LaunchCameraActivity" />
        <activity android:name=".CameraActivity" />
        <activity
            android:name=".ShowImgActivity"
            android:hardwareAccelerated="false" />
        <activity
            android:name=".ImageEvaluate"
            android:hardwareAccelerated="false" />
    </application>

</manifest>